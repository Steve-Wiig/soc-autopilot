# SOC-Autopilot Environment Configuration
# Copy this file to .env and fill in the values.
# NEVER commit real secrets to the repository.

# Development/Cloud LLM Access (NOT used for production SOC triage)
OPENROUTER_API_KEY=your_openrouter_key_here

# Testing Overrides (Do not set in production)
# SOC_MOCK_LLM_URL=http://localhost:15432
